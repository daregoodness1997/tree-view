import { Typography, styled } from "@mui/material";

const StyledTreeItemLabelText = styled(Typography)({
  color: "inherit",
  fontWeight: 500,
}) as unknown as typeof Typography;

export default StyledTreeItemLabelText;
